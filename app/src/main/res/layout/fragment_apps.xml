<?xml version="1.0" encoding="utf-8"?>
<!--
  The grid sits underneath the state view so the empty/loading art can cover it
  without the two fighting for the same space (they used to be siblings that both
  filled the parent, which left the recycler drawing over the empty state).
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

    <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recyclerView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:clipToPadding="false"
            android:paddingStart="8dp"
            android:paddingTop="4dp"
            android:paddingEnd="8dp"
            android:paddingBottom="104dp"
            android:scrollbars="none" />

    <com.github.nukc.stateview.StateView
            android:id="@+id/stateView"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />

</FrameLayout>
